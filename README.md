# Codex 灵兽小筑

这是一个 Codex 灵兽桌宠的浏览器 demo。它把 Codex 使用行为翻译成修仙设定：

- Token / 用量：灵气
- 5 小时剩余量：短期灵脉
- 1 周剩余量：长期灵脉
- 本地项目：洞府
- GitHub 仓库：宗门藏经阁
- Commit：刻印玉简
- Push：上交宗门
- 拖入文件：投喂灵材

## 运行方式

直接双击打开 `index.html` 即可体验。这个模式不会写入项目文件，但可以下载投喂玉简。

如果你想让 demo 真的在项目里生成投喂玉简，请在这个文件夹中运行：

```powershell
python server.py
```

然后访问：

```text
http://localhost:3000
```

## 桌面悬浮窗

最小桌面版可以直接运行：

```powershell
python desktop_pet.py
```

它会显示一个置顶小窗，读取 Codex 短期/长期灵脉，每 60 秒自动刷新。窗口里有一个参考五阶段设定制作的像素风饕餮：会空闲呼吸、眨眼、点击互动、投喂文件、炼化成功，并随投喂/炼化次数进入幼体、开灵、筑基、金丹、化神五个阶段。

桌面版包含对话气泡、炼化炉进度条和灵兽日志。投喂时会打开文件选择框，选择文件后生成玉简并显示投喂/炼化动画。

界面已加入阶段徽章、状态小牌、自绘像素灵脉条和更完整的像素舞台，用来靠近五阶段灵兽设定图的产品感。

当前桌面版路线参考 Bongo Cat：先追求程序化生命感。点击窗口后按键盘，会触发“敲玉简”的左右爪动作、身体轻微晃动和敲击火花。

也可以在桌面版里开启“全局输入感应”。开启后，即使你在别的软件里打字或点击鼠标，饕餮也会跟着敲玉简。这个功能只感应输入发生，不记录具体按键内容，且默认关闭。

桌面版现在支持右上角的小窗按钮。开启后会收起详细面板，只保留灵兽和简短状态气泡，更接近常驻桌宠。长时间没有互动时，饕餮会闭眼打坐/犯困；点击、键盘输入、鼠标输入或投喂都会把它唤醒。

桌宠已经开始使用更清晰的生命状态：待机呼吸、敲玉简、被摸醒、吞灵材、炼化中、发光开心、灵脉偏低和打坐犯困。投喂后会先吞下灵材，再进入短暂的肚中炼化循环。

桌面版投喂会把玉简写入：

```text
pet-inbox/
```

这个目录默认不会上传 GitHub。

## 美术方向

当前桌宠沿用“美术”对话里的方向：简化像素桌宠、厚深色描边、Q 版比例、小尺寸可读；主色是玉绿色身体、奶油色肚皮、古金色小角、珊瑚色腮红。后续五阶段升级和装扮系统也会基于这个风格继续扩展。

## 动画 Demo

可以直接打开：

```text
animation-demo.html
```

这个页面演示“灵材投入、饕餮吞下、肚中炼化、吐出玉简”的循环动画，用的是同一套像素灵兽美术方向。

## 美术资源包

第一版美术资源已部署到：

```text
assets/art/
```

里面包含五阶段饕餮参考图、静态饕餮 SVG 标记、桌宠生命状态素材板、玉简图标、颜色 token 和资源清单。启动本地服务后可以访问：

```text
http://localhost:3000/art-kit.html
```

这一步的目的不是替换桌宠程序绘制，而是建立稳定的美术素材入口：程序化桌宠继续负责“生命感”，资源包负责统一风格、封面、README 展示图、分享卡和后续 Figma/Canva 设计。

桌面版现在也会读取 `assets/art/states.json`，把当前动作映射到对应的美术状态标签；这为后续逐步替换精灵图、做装扮锚点和状态素材检查打基础。
同时新增 `assets/art/state-glyphs.json`，为每个生命状态提供一个 7x7 的小像素图标；桌面版和美术资源页会共同读取它，作为真正素材接入的第一步。

资源包还新增 `assets/art/cosmetic-anchors.json`，先固定头部、胸颈和背部三个装扮锚点。桌面版会读取这些锚点并在互动状态下轻量显示，方便后续扩展帽子、玉佩、背包等装扮。

浏览器 demo 的顶部和控制台里也加入了“美术资源”入口，方便从产品页面直接查看资源包。

资源页会读取 `assets/art/states.json` 并生成状态说明卡片，便于把程序状态、动画触发条件和视觉设计统一起来。

资源页也会读取 `assets/art/manifest.json` 展示资源清单，方便后续设计、展示图和打包流程直接找到对应文件。

资源清单会为图片/SVG 显示缩略图，并提供“复制路径”按钮，方便后续在代码、Figma、Canva 或文档里引用。

## 项目记忆与设计工具

当前已安装 Figma 和 Canva 插件，用于后续 UI 设计、组件规范、项目封面、灵兽图鉴和宣传素材。

同时创建了自定义 Codex skill：

```text
codex-spirit-companion
```

它用于保持世界观、美术方向、隐私边界、架构路线和 Obsidian 同步规则一致。

关键项目设定会同步到 Obsidian：

```text
C:\Users\TonyT\Documents\obsidian\Codex 灵兽桌宠.md
```

## 第一代产品闭环

第一代产品不追求功能堆满，而是验证核心闭环：

```text
真实灵脉同步 -> 灵材投喂 -> 有效炼化 -> 玉简入洞府
```

浏览器 demo 里已经加入“一代闭环”面板，用来阶段性审查项目是否偏离方向。

详细审查记录见：

```text
docs/v1-product-audit.md
```

## 当前 demo 包含

- Q 版饕餮灵兽
- 开灵仪式和灵根选择
- 灵气余量面板
- 启动本地服务时尝试读取 Codex 真实用量
- 饱腹值、消化率、投喂次数、修为境界
- 文件拖放投喂
- 文本类文件初步摘要
- 投喂玉简下载
- 通过 `server.py` 写入 `pet-inbox/`
- 本地浏览器状态保存

## 隐私说明

这个 demo 默认只在浏览器本地运行，不上传文件。拖入文本文件时，只在浏览器里读取前一小段内容用于生成摘要。图片、PDF、压缩包等二进制文件不会读取正文，只记录文件名、类型、大小和投喂时间。使用 `server.py` 时，服务只监听 `127.0.0.1`，并只把生成的 Markdown 玉简写入当前项目的 `pet-inbox/`。

`pet-inbox/` 已被 `.gitignore` 忽略，避免把本地投喂记录误传到 GitHub。

## Codex 用量读取

使用 `python server.py` 启动时，demo 会尝试通过本地 `codex app-server` 调用 `account/rateLimits/read`，读取 5 小时和 1 周用量窗口。

如果本机没有可用的 `codex` 命令、未登录、权限受限，或 app-server 协议变更，页面会自动保留 demo 用量，不影响其他功能。
